---
description: 提交到 GitHub 时同步维护根目录与子目录 README 索引
alwaysApply: true
---

# Automation 仓库：README 与子目录文档

当用户要求将本仓库（`Automation/`）**提交或推送到 GitHub** 时，在 `git commit` 之前必须完成：

1. **根目录 `README.md`**
   - 维护「子项目索引」表格：覆盖仓库根下**每一个一级子目录**（业务目录）。
   - 为每个目录保留一行：**目录链接**、**一句话说明**、指向该目录 **`README.md`** 的链接。
   - 若新增、重命名或删除一级子目录，更新表格与链接，避免失效。

2. **一级子目录 `README.md`**
   - 每个承载业务的一级子目录下都应有 **`README.md`**。
   - 内容至少包含：用途、当前实现状态、如何运行或依赖说明（若尚未实现则写「规划/待办」）。
   - 若本次变更涉及该子项目行为，同步更新该子目录的 `README.md`。

3. **自检**
    - 提交前快速检查：是否存在「有目录但无对应 README」或「根索引未列出该目录」的情况；有则先补文档再提交。
    - 提交或推送远程仓库时，不允许为了脱敏远程内容而清空、脱敏、覆盖本地真实配置文件；本地配置必须保持真实可用。
    - 含账号、密码、API Key、Webhook、Cookie、Token、本机路径等敏感或本地专属信息的配置，应通过 `.gitignore`、示例配置、临时副本或路径限定暂存处理，不得把本地配置改成占位值后直接留在本地。

不要求为 `.git`、`.cursor` 等元数据目录写业务 README。
